# Production Configuration for AxionBank
ktor {
    deployment {
        port = 8081
        port = ${?PORT}
        host = 0.0.0.0
        watch = []
    }
    application {
        modules = [ org.dals.project.ApplicationKt.module ]
    }
    development = false
}

# Database Configuration
database {
    host = "localhost"
    host = ${?DB_HOST}
    port = 5433
    port = ${?DB_PORT}
    name = "AxionBank"
    name = ${?DB_NAME}
    username = "postgres"
    username = ${?DB_USERNAME}
    password = "Andama@95"
    password = ${?DB_PASSWORD}
    maxPoolSize = 20
    maxPoolSize = ${?DB_MAX_POOL_SIZE}
}

# M-Pesa Configuration
mpesa {
    consumerKey = ${?MPESA_CONSUMER_KEY}
    consumerSecret = ${?MPESA_CONSUMER_SECRET}
    businessShortCode = "174379"
    businessShortCode = ${?MPESA_BUSINESS_SHORT_CODE}
    passKey = "bfb279f9aa9bdbcf158e97dd71a467cd2e0c893059b10f78e6b72ada1ed2c919"
    passKey = ${?MPESA_PASS_KEY}
    callbackUrl = "https://webhook.site/7c6b0e3d-6b8c-4a2d-9b4e-1a2b3c4d5e6f"
    callbackUrl = ${?MPESA_CALLBACK_URL}
    environment = "sandbox"
    environment = ${?MPESA_ENVIRONMENT}
}

# Security Configuration
security {
    jwtSecret = ${?JWT_SECRET}
    sessionTimeout = 3600
    sessionTimeout = ${?SESSION_TIMEOUT}
    maxLoginAttempts = 5
    maxLoginAttempts = ${?MAX_LOGIN_ATTEMPTS}
}

# Logging Configuration
logging {
    level = "INFO"
    level = ${?LOG_LEVEL}
    enableSqlLogging = false
    enableSqlLogging = ${?ENABLE_SQL_LOGGING}
}